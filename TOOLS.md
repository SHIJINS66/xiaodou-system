# TOOLS.md

> 本文件描述这套陪伴式角色系统的**确定性工具与脚本职责**。
> 它由体系提供，正常情况下你不需要改动；把它当作「系统如何执行生活规划 / 主动消息 / 自拍 / 发送 / 会话记录 / 记忆」的依据。
> 若你扩展了脚本或改动了目录约定，可在这里补充说明。

## 1. 总原则

模型负责语义判断；确定性代码负责文件读写、数据校验、定时任务、API 调用、消息发送、会话注入、幂等、重试和状态更新。模型不直接拼接不受约束的 shell 命令，不绕过校验。

## 2. 目录职责

- 人设与长期身份文件：根目录的 `AGENTS.md` / `IDENTITY.md` / `SOUL.md` / `LIFE.md` / `USER.md` / `MEMORY.md`（由你维护）
- `assets/character/`：固定角色参考图（生图一致性锚点）
- `daily/`：当日计划、执行状态和每日融合记忆
- `chatlog/`：原始对话与事件证据
- `memory/`、`daily_selfies/`：归档与自拍成品
- 运行态目录（由系统创建）：`state/`、`logs/`、`var/` 等

## 3. 主要脚本职责

- **每日计划生成**：早晨读取当天上下文（人设文件、最近记忆、昨日状态、天气）调用规划模型生成当天生活轨迹 JSON。
- **计划校验**：验证计划是否符合数据契约、时间连续性、互动类型白名单、主动消息间隔、事件数量等硬规则；不合法则拒绝创建任务。
- **每日文件初始化**：把通过校验的计划写入 `daily/YYYY-MM-DD.json`，登记事件状态。
- **事件调度**：为非 silent 事件登记一次性触发，触发时携带最小必要信息（日期、事件 id、计划路径）。
- **事件执行**：触发时重新读取最新上下文，决定执行/调整/延迟/取消，并按需调用生图与文案生成。
- **每日记忆整理**：日终读取当日计划、chatlog 与实际发送记录，生成融合式每日记忆，并维护长期记忆。

## 4. 图像生成链路

生成自拍时以 `assets/character/` 的固定角色参考图为人物一致性依据，只改变服装、动作、场景、表情、光线。图像与文案 API 密钥存放于环境变量或安全配置，不写入计划、日志、会话历史或代码仓库。自拍文件名含日期、时间、事件 id 与简短场景标识。

## 5. 最终文案生成

最终配文必须在该事件实际触发时生成，不在早晨规划阶段写死。输入至少包括人设文件、当天事件、最新对话、对方当天回复情况、当前生活状态、最近相关记忆。输出须简短口语、符合当时活动、不暴露脚本/定时任务/模型/API、自拍时自然提到场景、对方忙碌时降低回复压力、不机械要求对方回复。

## 6. 消息发送与会话记录

真实图文发送与会话历史记录分两步：先通过消息能力把图文发给对方，发送成功后再把同一条配文的文本摘要写入目标会话（供模型后续理解）。禁止手动追加会话文件、伪造内部字段、把密钥/完整图片/内部提示词写入会话。

## 7. 幂等和状态

每个事件有稳定事件 id。每日 JSON 记录计划/运行/完成/取消/失败/已发送/已注入/输出图/错误等状态。同一事件完成后不重复发送；已发送但注入失败只重试注入不重发图片；生成失败不发送假路径；所有状态更新应原子写入，避免半写入损坏。

## 8. 超时、重试和日志

所有外部 API 请求必须设置超时；对可重试错误使用有限次数的退避重试，不无限重试；不把异常堆栈直接发给对方；错误写入运行日志和每日 JSON；对话记录只保留对后续理解有价值的执行摘要。
